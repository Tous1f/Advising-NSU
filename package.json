{
  "name": "batsched",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "python -m http.server 5500",
    "build": "echo no build step (static)",
    "cap:init": "npx cap init batsched com.example.batsched --web-dir=."
  },
  "devDependencies": {
    "@capacitor/cli": "^5.0.0",
    "@capacitor/core": "^5.0.0"
  }
}
